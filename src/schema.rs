table! {
    students (id) {
        id -> Int4,
        first_name -> Varchar,
        last_name -> Varchar,
        department -> Varchar,
        is_graduated -> Bool,
        age -> Int4,
    }
}
